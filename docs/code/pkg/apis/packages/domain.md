<!-- Code generated by gomarkdoc. DO NOT EDIT -->

# domain

```go
import "github.com/ntlaletsi70/blanketops-environments/pkg/apis/packages/domain"
```

## Index

- [type ApplicationPhase](<#ApplicationPhase>)
- [type ApplicationState](<#ApplicationState>)
- [type ApplyStrategy](<#ApplyStrategy>)
- [type DiffRequiredError](<#DiffRequiredError>)
  - [func \(e DiffRequiredError\) Error\(\) string](<#DiffRequiredError.Error>)
  - [func \(e DiffRequiredError\) Reason\(\) string](<#DiffRequiredError.Reason>)
  - [func \(e DiffRequiredError\) Retryable\(\) bool](<#DiffRequiredError.Retryable>)
- [type DiffState](<#DiffState>)
- [type DriftDetectedError](<#DriftDetectedError>)
  - [func \(e DriftDetectedError\) Error\(\) string](<#DriftDetectedError.Error>)
  - [func \(e DriftDetectedError\) Reason\(\) string](<#DriftDetectedError.Reason>)
  - [func \(e DriftDetectedError\) Retryable\(\) bool](<#DriftDetectedError.Retryable>)
- [type InvalidSpecError](<#InvalidSpecError>)
  - [func \(e InvalidSpecError\) Error\(\) string](<#InvalidSpecError.Error>)
  - [func \(e InvalidSpecError\) Reason\(\) string](<#InvalidSpecError.Reason>)
  - [func \(e InvalidSpecError\) Retryable\(\) bool](<#InvalidSpecError.Retryable>)
- [type KappAction](<#KappAction>)
- [type KappExecutionError](<#KappExecutionError>)
  - [func \(e KappExecutionError\) Error\(\) string](<#KappExecutionError.Error>)
  - [func \(e KappExecutionError\) Reason\(\) string](<#KappExecutionError.Reason>)
  - [func \(e KappExecutionError\) Retryable\(\) bool](<#KappExecutionError.Retryable>)
- [type KappResult](<#KappResult>)
- [type Maintainer](<#Maintainer>)
- [type PackageError](<#PackageError>)
- [type PackageID](<#PackageID>)
- [type PackagePhase](<#PackagePhase>)
- [type PackageResult](<#PackageResult>)
- [type PackageSource](<#PackageSource>)
- [type PackageSpec](<#PackageSpec>)
- [type PackageState](<#PackageState>)
- [type PackageStatus](<#PackageStatus>)
- [type Ref](<#Ref>)
- [type RepositoryError](<#RepositoryError>)
  - [func \(e RepositoryError\) Error\(\) string](<#RepositoryError.Error>)
  - [func \(e RepositoryError\) Reason\(\) string](<#RepositoryError.Reason>)
  - [func \(e RepositoryError\) Retryable\(\) bool](<#RepositoryError.Retryable>)
- [type StateRepository](<#StateRepository>)


<a name="ApplicationPhase"></a>
## type ApplicationPhase



```go
type ApplicationPhase string
```

<a name="ApplicationPhasePending"></a>

```go
const (
    ApplicationPhasePending ApplicationPhase = "Pending"
    ApplicationPhaseReady   ApplicationPhase = "Ready"
    ApplicationPhaseFailed  ApplicationPhase = "Failed"
)
```

<a name="ApplicationState"></a>
## type ApplicationState



```go
type ApplicationState struct {
    Name      string
    Namespace string

    Phase   ApplicationPhase
    Message string

    // Deploy timing (authoritative)
    DeployStartedAt *time.Time
    DeployUpdatedAt *time.Time

    // Exit info
    DeployFinished bool
    DeployExitCode *int
}
```

<a name="ApplyStrategy"></a>
## type ApplyStrategy



```go
type ApplyStrategy string
```

<a name="StrategyKustomize"></a>

```go
const (
    StrategyKustomize ApplyStrategy = "kustomize"
    StrategyPlainYAML ApplyStrategy = "plain"
)
```

<a name="DiffRequiredError"></a>
## type DiffRequiredError



```go
type DiffRequiredError struct {
    Msg string
}
```

<a name="DiffRequiredError.Error"></a>
### func \(DiffRequiredError\) Error

```go
func (e DiffRequiredError) Error() string
```



<a name="DiffRequiredError.Reason"></a>
### func \(DiffRequiredError\) Reason

```go
func (e DiffRequiredError) Reason() string
```



<a name="DiffRequiredError.Retryable"></a>
### func \(DiffRequiredError\) Retryable

```go
func (e DiffRequiredError) Retryable() bool
```



<a name="DiffState"></a>
## type DiffState



```go
type DiffState string
```

<a name="DiffUnknown"></a>

```go
const (
    DiffUnknown  DiffState = "Unknown"
    DiffClean    DiffState = "Clean"
    DiffDetected DiffState = "Detected"
    DiffApplied  DiffState = "Applied"
)
```

<a name="DriftDetectedError"></a>
## type DriftDetectedError



```go
type DriftDetectedError struct {
    Msg string
}
```

<a name="DriftDetectedError.Error"></a>
### func \(DriftDetectedError\) Error

```go
func (e DriftDetectedError) Error() string
```



<a name="DriftDetectedError.Reason"></a>
### func \(DriftDetectedError\) Reason

```go
func (e DriftDetectedError) Reason() string
```



<a name="DriftDetectedError.Retryable"></a>
### func \(DriftDetectedError\) Retryable

```go
func (e DriftDetectedError) Retryable() bool
```



<a name="InvalidSpecError"></a>
## type InvalidSpecError



```go
type InvalidSpecError struct {
    Msg string
}
```

<a name="InvalidSpecError.Error"></a>
### func \(InvalidSpecError\) Error

```go
func (e InvalidSpecError) Error() string
```



<a name="InvalidSpecError.Reason"></a>
### func \(InvalidSpecError\) Reason

```go
func (e InvalidSpecError) Reason() string
```



<a name="InvalidSpecError.Retryable"></a>
### func \(InvalidSpecError\) Retryable

```go
func (e InvalidSpecError) Retryable() bool
```



<a name="KappAction"></a>
## type KappAction



```go
type KappAction string
```

<a name="KappActionNone"></a>

```go
const (
    KappActionNone  KappAction = "None"
    KappActionDiff  KappAction = "Diff"
    KappActionApply KappAction = "Apply"
)
```

<a name="KappExecutionError"></a>
## type KappExecutionError



```go
type KappExecutionError struct {
    Action KappAction
    Output string
    Msg    string
}
```

<a name="KappExecutionError.Error"></a>
### func \(KappExecutionError\) Error

```go
func (e KappExecutionError) Error() string
```



<a name="KappExecutionError.Reason"></a>
### func \(KappExecutionError\) Reason

```go
func (e KappExecutionError) Reason() string
```



<a name="KappExecutionError.Retryable"></a>
### func \(KappExecutionError\) Retryable

```go
func (e KappExecutionError) Retryable() bool
```



<a name="KappResult"></a>
## type KappResult



```go
type KappResult struct {
    // Executed indicates whether kapp was invoked at all.
    Name      string
    Namespace string
    Executed  bool

    // Deploy timing (authoritative)
    DeployStartedAt *time.Time
    DeployUpdatedAt *time.Time

    // Exit info
    DeployFinished bool
    DeployExitCode *int

    // Action taken by kapp.
    // e.g. diff, apply, noop
    Action KappAction

    // Diff indicates whether differences were detected.
    Diff DiffState

    // Output is raw kapp stdout/stderr (trimmed upstream if needed).
    Output string

    // Error captures execution failure, if any.
    Error string
}
```

<a name="Maintainer"></a>
## type Maintainer



```go
type Maintainer struct {
    Name  string
    Email string
}
```

<a name="PackageError"></a>
## type PackageError



```go
type PackageError interface {
    Reason() string
    Retryable() bool
    // contains filtered or unexported methods
}
```

<a name="PackageID"></a>
## type PackageID



```go
type PackageID struct {
    Namespace string
    Name      string
}
```

<a name="PackagePhase"></a>
## type PackagePhase



```go
type PackagePhase string
```

<a name="PackagePhasePending"></a>

```go
const (
    PackagePhasePending     PackagePhase = "Pending"
    PackagePhaseApplied     PackagePhase = "Applied"
    PackagePhaseDrifted     PackagePhase = "Drifted"
    PackagePhaseFailed      PackagePhase = "Failed"
    PackagePhaseDisabled    PackagePhase = "Disabled"
    PackagePhaseReconciling PackagePhase = "Reconciling"
    PackagePhaseReady       PackagePhase = "Ready"
    PackagePhaseSucceeded   PackagePhase = "Succeeded"
    PackagePhaseUnknown     PackagePhase = "Unknown"
)
```

<a name="PackageResult"></a>
## type PackageResult

PackageResult represents the outcome of a single reconciliation run.

```go
type PackageResult struct {
    Success bool
    Phase   PackagePhase
    Message string

    // DesiredRef is what the controller intended to apply.
    DesiredRef string

    // DesiredCommit is the resolved commit hash.
    DesiredCommit string

    Kapp KappResult

    StartedAt  time.Time
    FinishedAt time.Time
}
```

<a name="PackageSource"></a>
## type PackageSource



```go
type PackageSource struct {
    // Repository URL containing the package manifests.
    RepositoryURL string

    // CredentialsSecret references auth material (opaque to domain).
    CredentialsSecret string
}
```

<a name="PackageSpec"></a>
## type PackageSpec

Package represents the domain\-level truth of a Package. It is independent of Kubernetes API machinery.

```go
type PackageSpec struct {

    // ID is a stable internal identifier (e.g. namespace/name).
    ID  PackageID

    // Name is the human-readable package name.
    Name string

    // Version is the declared package version.
    Version string

    // Enabled determines whether reconciliation is active.
    Enabled bool

    // Description provides human context.
    Description string

    // Maintainers represent ownership and accountability.
    Maintainers []Maintainer

    // Source defines where manifests come from.
    Source PackageSource

    // StateRepo defines where rendered / applied state is tracked.
    StateRepo StateRepository

    // DiffEnabled determines whether kapp diff must be executed.
    DiffEnabled bool

    // Strategy defines how manifests are applied (e.g. kustomize).
    Strategy ApplyStrategy

    // LastReconciledAt records the last reconciliation attempt.
    LastReconciledAt *time.Time
}
```

<a name="PackageState"></a>
## type PackageState

PackageState represents the observed state of a Package.

```go
type PackageState struct {
    Phase PackagePhase

    // Message provides human-readable context.
    Message string

    // AppliedRef is the git ref (branch/tag/commit) last applied.
    AppliedRef string

    // AppliedCommit is the exact commit hash applied.
    AppliedCommit string

    // Diff indicates whether kapp detected differences.
    Diff DiffState

    // LastDiff contains the most recent kapp diff output (if enabled).
    LastDiff string

    LastAppliedAt *time.Time
    LastCheckedAt *time.Time
}
```

<a name="PackageStatus"></a>
## type PackageStatus

PackageStatus is the USER\-FACING contract status. Stable, backend\-agnostic, derived from PackageResult.

```go
type PackageStatus struct {
    Phase   PackagePhase
    Success bool
    Message string

    DesiredRef    string
    DesiredCommit string

    // Executed indicates whether a deploy tool (kapp) was invoked.
    Executed bool

    // KappAction represents what kapp actually did.
    // This is promoted intentionally (Diff / Apply / None).
    KappAction KappAction

    // Diff represents the diff outcome observed by kapp.
    Diff DiffState

    StartedAt  time.Time
    FinishedAt time.Time
}
```

<a name="Ref"></a>
## type Ref



```go
type Ref struct {

    // Branch is the Git branch to use for the manifests repository.
    Branch string

    // Tag is the Git tag to use for the manifests repository.
    Tag string

    // Commit is the specific Git commit SHA to use for the manifests repository.
    Commit string
}
```

<a name="RepositoryError"></a>
## type RepositoryError



```go
type RepositoryError struct {
    RepoURL string
    Msg     string
}
```

<a name="RepositoryError.Error"></a>
### func \(RepositoryError\) Error

```go
func (e RepositoryError) Error() string
```



<a name="RepositoryError.Reason"></a>
### func \(RepositoryError\) Reason

```go
func (e RepositoryError) Reason() string
```



<a name="RepositoryError.Retryable"></a>
### func \(RepositoryError\) Retryable

```go
func (e RepositoryError) Retryable() bool
```



<a name="StateRepository"></a>
## type StateRepository



```go
type StateRepository struct {
    // URL of the state/manifests repository.
    URL string

    // Ref (branch, tag, or commit).
    Ref Ref

    // CloneSecret used to authenticate.
    CloneSecret string

    // Strategy ImageUpdateAutomation rule(i.e, how to apply our manifests from the repo to our target cluster).
    Strategy string

    // Path is the path of the kustomization file when kustomization strategy = true.
    Path string
}
```

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
